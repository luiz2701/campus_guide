/*
  Constantes de rotas nomeadas usadas pela aplicação.

  Mantenha as rotas aqui (fonte única). Use `AppRoutes.<nome>` ao invés de
  escrever strings literais para evitar erros e facilitar refactors.
*/

class AppRoutes {
  static const home = "/";
  static const profile = "/profile";
  static const editProfile = "/edit-profile";
  static const login = "/login";
  static const register = "/register";
  static const eventDetails = "/event-details";
}
